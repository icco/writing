name: Archive some posts
on:
  schedule:
    - cron: 12 */6 * * *
  workflow_dispatch:
jobs:
  get_posts:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code.
        uses: actions/checkout@v6
      - name: Install zsh
        run: sudo apt-get update; sudo apt-get install zsh
      - name: Retrieve all posts
        run: |
          POSTS=$(./all-posts.sh 25)
          echo "posts=$POSTS" >> $GITHUB_OUTPUT
        id: posts
    outputs:
      posts: ${{ steps.posts.outputs.posts }}
  archive:
    needs: get_posts
    if: needs.get_posts.outputs.posts != '[]'
    strategy:
      fail-fast: false
      matrix:
        url: ${{ fromJson(needs.get_posts.outputs.posts) }}
    runs-on: ubuntu-latest
    steps:
      - uses: JamieMagee/wayback@09d9eb335b234c48032a90b5d630e1723a62982c # main
        with:
          url: ${{ matrix.url }}
          saveErrors: false
          saveOutlinks: true
          saveScreenshot: true
