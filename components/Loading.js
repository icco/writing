import React from "react";
import ReactLoading from "react-loading";

const Loading = () => (
  <ReactLoading type={"bars"} height={64} width={64} className="center grey" />
);

export default Loading;
